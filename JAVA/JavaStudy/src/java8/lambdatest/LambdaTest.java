package java8.lambdatest;

public class LambdaTest {
    public static void main(String[] args) {
        /*
        * 1. 람다표현식의 기본 문법
        * (매개변수)->{실행문;}
        * 2. 람다표현식 특징
        *  - 매개변수가 하나면 괄호를 생략할 수 있다. 단, 매개변수 타입이 있으면 괄호를 생략할 수 없다.
        *  - {} 안의 문장이 return 문 일 경우에는 {} 를 생략할 수 없다.
        * 3. 람다표현식의 사용
        *  - 람다 표현식을 사용할 수 있는 곳은 다음과 같다.
        *  1) 함수 인터페이스의 인수로 전달
        *  2)
        * */

    }
}
